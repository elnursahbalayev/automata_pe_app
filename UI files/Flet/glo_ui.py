from Backend.gasLiftOptimization import GasLiftOptimization
import flet as ft
import plotly.io as pio

class GLOUi:
    def __init__(self, page):
        # self.page = page
        # self.glo = GasLiftOptimization()
        pass

    def upload_ui(self):
        # # Create a container for the UI elements
        # container = ft.Column(spacing=20)
        #
        # # Process the data
        # self.glo.check_dir_and_retrieve_values(1.5, 0.05)
        # self.glo.simulation()
        # self.glo.wells()
        # self.glo.generate_well_list()
        # self.glo.testing()
        #
        # # Convert Plotly figure to HTML and display it in a WebView
        # visualization_fig = self.glo.visualize()
        # visualization_html = pio.to_html(visualization_fig, full_html=False)
        # visualization_view = ft.WebView(
        #     html=visualization_html,
        #     width=800,
        #     height=500
        # )
        #
        # # Add the plot to the container
        # container.controls.extend([
        #     ft.Text("Gas Lift Optimization Visualization", size=20, weight=ft.FontWeight.BOLD),
        #     visualization_view
        # ])
        #
        # return container
        pass